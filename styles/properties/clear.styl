$TD-clearfix {
  &::after {
    content: '';
    display: block;
    clear: both;
  }
}

clear(value) {
  if (value == fix or value == fixed) {
    @extend $TD-clearfix;
  } else {
    clear: value;
  }
}
