$TD-overflow-ellipsis {
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
}

overflow(value) {
  if value == ellipsis {
    @extend $TD-overflow-ellipsis;
  } else {
    overflow: value;
  }
}
